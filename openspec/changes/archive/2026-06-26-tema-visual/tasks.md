# Tasks: Tema Visual

## 1. Cores

- [x] 1.1 Adicionar `@theme {}` em `globals.css` redefinindo `--color-rose-*` (marrom) e `--color-pink-*` (mostarda)
- [x] 1.2 Verificar que nenhum componente usa valores hardcoded fora das classes Tailwind

## 2. Fonte

- [x] 2.1 Substituir `Inter` por `Montserrat` (pesos 400, 500, 600, 700) em `app/layout.tsx`
- [x] 2.2 Configurar com `variable: '--font-sans'` para alimentar o `@theme inline` existente
